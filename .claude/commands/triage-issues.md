# Triage Issues

Use `docs/agent-workflows/triage-issues.md` as the canonical workflow.

Arguments: `$ARGUMENTS`

Begin with:

```text
Matched workflow: triage issues.
```

Then follow the canonical workflow exactly.
