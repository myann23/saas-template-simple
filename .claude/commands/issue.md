# Work on GitHub Issue

Use `docs/agent-workflows/issue.md` as the canonical workflow.

Issue number: `$ARGUMENTS`

Begin with:

```text
Matched workflow: issue $ARGUMENTS.
```

Then follow the canonical workflow exactly, substituting `$ARGUMENTS` wherever the workflow says `<number>`.
