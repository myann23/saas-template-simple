# Verify

Use `docs/agent-workflows/verify.md` as the canonical workflow.

Mode or arguments: `$ARGUMENTS`

Begin with:

```text
Matched workflow: verify.
```

Then follow the canonical workflow exactly.
