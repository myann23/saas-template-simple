---
name: checkpoint
description: Create, verify, list, or clear workflow checkpoints. Use when Mike says "$checkpoint", "/checkpoint", "create checkpoint", "verify checkpoint", or asks to snapshot work progress.
---

# Checkpoint

Start with:

```text
Matched workflow: checkpoint.
```

Use `docs/agent-workflows/checkpoint.md` as the canonical workflow.
