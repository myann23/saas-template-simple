---
name: plan
description: Produce an implementation plan before coding. Use when Mike says "$plan", "/plan", "plan this", asks for an implementation plan, or asks Codex to think through architecture before editing.
---

# Plan

Start with:

```text
Matched workflow: plan.
```

Use `docs/agent-workflows/plan.md` as the canonical workflow.
