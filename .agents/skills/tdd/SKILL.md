---
name: tdd
description: Follow test-driven development. Use when Mike says "$tdd", "/tdd", "TDD", asks for a test-first implementation, or asks to write a reproducing test before fixing a bug.
---

# TDD

Start with:

```text
Matched workflow: tdd.
```

Use `docs/agent-workflows/tdd.md` as the canonical workflow.
