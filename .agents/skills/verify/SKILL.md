---
name: verify
description: Run repository verification checks. Use when Mike says "$verify", "/verify", "verify", "run checks", "run tests", or asks whether the repo is ready to commit or PR.
---

# Verify

Start with:

```text
Matched workflow: verify.
```

Use `docs/agent-workflows/verify.md` as the canonical workflow.
